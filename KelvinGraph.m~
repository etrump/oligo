global modelAtm

modelAtm.Pop88.shapeFact = 1;
nm = zeros(1,1)*NaN;
Ke = zeros(1,1)*NaN;

for i = 10:100
    nm(i) = i;
    m(i) = i*1e-9;
    modelAtm.Pop88.Dp = m(i);
    
    Ke(i) = KelvinTerm(88);
end

figu
loglog(nm,Ke)
hold on
loglog(1)

Ke(5)